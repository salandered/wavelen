openapi: 3.0.4
info:
  title: Wavelen API
  version: 1.0.0
  description: >-
    Just saving colors, why should we not

tags:
  - name: System
    description: Operational
  - name: Users
    description: User accounts
  - name: Colors
    description: A user's own saved colors
  - name: Palette
    description: The shared read-only palette
  - name: Harmony
    description: Colors computed from a color. Nothing is read or stored.

paths:
  /:
    get:
      tags: [System]
      summary: Returns the service name and build-time version
      responses:
        "200":
          description: Successful response
          content:
            text/plain:
              schema:
                type: string
                example: "wavelen version 0.1.0"

  /livez:
    get:
      tags: [System]
      summary: Checks if the service is alive
      responses:
        "200":
          description: The process is up
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"

  /readyz:
    get:
      tags: [System]
      summary: Checks if the service can reach its database
      responses:
        "200":
          description: The service and Postgres are ready
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"
        "503":
          description: Postgres is unreachable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DependencyUnavailableResponse"

  /api/v1/users:
    post:
      tags: [Users]
      summary: Creates a user
      description: >-
        The email is trimmed and lowercased before storage. Uniqueness ignores case.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateUserRequest"
      responses:
        "201":
          description: User created
          headers:
            Location:
              description: Path of the new user
              schema:
                type: string
                example: /api/v1/users/1
            X-Request-Id:
              $ref: "#/components/headers/XRequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateUserResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "409":
          description: The email is already registered
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                error: email already registered
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/users/{user_id}/colors:
    parameters:
      - $ref: "#/components/parameters/UserId"
    get:
      tags: [Colors]
      summary: Lists a user's saved colors
      description: >-
        Ordered by the `sort`. No `next_cursor` means the end of the list.
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
        - name: sort
          in: query
          required: false
          description: >-
            The page is ordered by it. hex is the tiebreak for created_at and for color.
            hex is lexicographic; color is perceptual
          schema:
            type: string
            enum: [created_at, hex, color]
            default: created_at
        - name: order
          in: query
          required: false
          schema:
            type: string
            enum: [asc, desc]
            default: desc
        - name: cursor
          in: query
          required: false
          schema:
            type: string
          example: "Y3JlYXRlZF9hdHxkZXNjfDIwMjYtMDgtMjNUMTQ6MDA6MDAuODI1MDIzWnwjZmYwMGFh"
      responses:
        "200":
          description: The user's colors
          headers:
            X-Request-Id:
              $ref: "#/components/headers/XRequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListColorsResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "500":
          $ref: "#/components/responses/InternalServerError"
    post:
      tags: [Colors]
      summary: Saves a color for a user
      description: >-
        Idempotent. Saving a color the user already has returns the same body with 200.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddColorRequest"
      responses:
        "201":
          description: The color was added
          headers:
            X-Request-Id:
              $ref: "#/components/headers/XRequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AddColorResponse"
        "200":
          description: The user had already saved this color
          headers:
            X-Request-Id:
              $ref: "#/components/headers/XRequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AddColorResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          description: No such user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                error: user not found
        "409":
          description: The user exceeded the color quota.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/users/{user_id}/colors/{hex}:
    parameters:
      - $ref: "#/components/parameters/UserId"
      - $ref: "#/components/parameters/ColorHex"
    delete:
      tags: [Colors]
      summary: Removes one of a user's saved colors
      responses:
        "204":
          description: The color was removed
          headers:
            X-Request-Id:
              $ref: "#/components/headers/XRequestId"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          description: The user doesn't have this color
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                error: not found
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/colors:
    get:
      tags: [Palette]
      summary: Lists the shared palette
      description: >-
        The 100 seeded named colors, ordered by the `sort`.
      parameters:
        - name: sort
          in: query
          required: false
          schema:
            type: string
            enum: [name, hex, color]
            default: name
        - name: order
          in: query
          required: false
          schema:
            type: string
            enum: [asc, desc]
            default: asc
      responses:
        "200":
          description: The palette
          headers:
            X-Request-Id:
              $ref: "#/components/headers/XRequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListCommonColorsResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/colors/{hex}/complement:
    parameters:
      - $ref: "#/components/parameters/ColorHex"
    get:
      tags: [Harmony]
      summary: The opposite color
      description: >-
        The hue turned half a circle in OkLCh, lightness and chroma kept. The hex needs not be
        one of the palette colors; any valid color has an answer, so this never returns 404 and
        cannot fail once the path parses. A gray has no hue to turn and is its own complement.
      responses:
        "200":
          description: The complement
          headers:
            X-Request-Id:
              $ref: "#/components/headers/XRequestId"
            Cache-Control:
              $ref: "#/components/headers/HarmonyCacheControl"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ComplementResponse"
        "400":
          $ref: "#/components/responses/BadRequest"

  /api/v1/colors/{hex}/triad:
    parameters:
      - $ref: "#/components/parameters/ColorHex"
    get:
      tags: [Harmony]
      summary: The two colors evenly spaced from this one
      description: >-
        The hue turned a third and two thirds of a circle in OkLCh, lightness and chroma kept.
        Same rules as the complement: any valid color has an answer, and a gray is answered with
        itself twice.
      responses:
        "200":
          description: The other two corners of the triangle
          headers:
            X-Request-Id:
              $ref: "#/components/headers/XRequestId"
            Cache-Control:
              $ref: "#/components/headers/HarmonyCacheControl"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TriadResponse"
        "400":
          $ref: "#/components/responses/BadRequest"

components:
  headers:
    XRequestId:
      description: >-
        Correlation id, always generated by the server. A client-sent X-Request-Id is ignored.
      schema:
        type: string

    HarmonyCacheControl:
      description: >-
        A harmony depends on the URL and the formula, and the formula only changes with a deploy.
      schema:
        type: string
      example: "public, max-age=31536000, immutable"

  parameters:
    UserId:
      name: user_id
      in: path
      required: true
      schema:
        type: integer
        format: int64
        minimum: 1
      example: 1

    ColorHex:
      name: hex
      in: path
      required: true
      description: >-
        A color, as six hex digits WITHOUT the leading '#'. Case does not matter.
        Responses carry the stored form with '#'.
      schema:
        type: string
        pattern: "^[0-9a-fA-F]{6}$"
      example: "ff00aa"

  schemas:
    HexStored:
      type: string
      description: A color code as stored and returned, lowercase with a leading '#'.
      pattern: "^#[0-9a-f]{6}$"
      example: "#ff00aa"

    HexInput:
      type: string
      description: >-
        A color code as accepted. Surrounding whitespace is trimmed, the leading '#' is
        optional, case does not matter. The normalized form is stored.
      pattern: "^\\s*#?[0-9a-fA-F]{6}\\s*$"
      example: "FF00AA"

    CreateUserRequest:
      type: object
      required: [email, name]
      additionalProperties: false
      properties:
        email:
          type: string
          format: email
          maxLength: 254
          example: olya@example.com
        name:
          type: string
          minLength: 1
          maxLength: 100
          description: Length is counted in runes, after trimming.
          example: Olya Lovelace

    User:
      type: object
      required: [id, email, name, created_at]
      properties:
        id:
          type: integer
          format: int64
          example: 1
        email:
          type: string
          format: email
          example: olya@example.com
        name:
          type: string
          example: Olya Lovelace
        created_at:
          type: string
          format: date-time
          example: "2026-08-22T13:37:08.688203Z"

    CreateUserResponse:
      type: object
      required: [user]
      properties:
        user:
          $ref: "#/components/schemas/User"

    AddColorRequest:
      type: object
      required: [hex]
      additionalProperties: false
      properties:
        hex:
          $ref: "#/components/schemas/HexInput"

    AddColorResponse:
      type: object
      required: [hex]
      description: Echoes the normalized hex
      properties:
        hex:
          $ref: "#/components/schemas/HexStored"

    SavedColor:
      type: object
      required: [hex, created_at]
      properties:
        hex:
          $ref: "#/components/schemas/HexStored"
        created_at:
          type: string
          format: date-time
          example: "2026-08-22T13:37:08.825023Z"

    CursorMetadata:
      type: object
      required: [limit]
      properties:
        limit:
          type: integer
          description: The page size
          example: 50
        next_cursor:
          type: string
          description: Absent at the end of the list.
          example: "Y3JlYXRlZF9hdHxkZXNjfDIwMjYtMDgtMjNUMTQ6MDA6MDAuODI1MDIzWnwjZmYwMGFh"

    ListColorsResponse:
      type: object
      required: [colors, metadata]
      properties:
        colors:
          type: array
          items:
            $ref: "#/components/schemas/SavedColor"
        metadata:
          $ref: "#/components/schemas/CursorMetadata"

    CommonColor:
      type: object
      required: [hex, name]
      properties:
        hex:
          $ref: "#/components/schemas/HexStored"
        name:
          type: string
          example: black

    ListCommonColorsResponse:
      type: object
      required: [colors]
      properties:
        colors:
          type: array
          items:
            $ref: "#/components/schemas/CommonColor"

    ComplementResponse:
      type: object
      required: [hex, complement]
      description: Echoes the normalized hex asked about, beside its complement.
      properties:
        hex:
          $ref: "#/components/schemas/HexStored"
        complement:
          $ref: "#/components/schemas/HexStored"

    TriadResponse:
      type: object
      required: [hex, triad]
      description: >-
        Echoes the normalized hex asked about, beside the OTHER two colors of the triad in hue
        order. The color asked about is not repeated in the array.
      properties:
        hex:
          $ref: "#/components/schemas/HexStored"
        triad:
          type: array
          minItems: 2
          maxItems: 2
          items:
            $ref: "#/components/schemas/HexStored"

    HealthResponse:
      type: object
      required: [status]
      additionalProperties: false
      properties:
        status:
          type: string
          enum: [ok]

    DependencyUnavailableResponse:
      type: object
      required: [status, dependency]
      additionalProperties: false
      properties:
        status:
          type: string
          enum: [unavailable]
        dependency:
          type: string
          enum: [postgres]

    ErrorResponse:
      type: object
      required: [error]
      properties:
        error:
          type: string
          description: For 5xx this is always the "internal server error"
          example: invalid hex color

  responses:
    BadRequest:
      description: Malformed path parameter, body, or field
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            error: 'invalid hex color: want 6 hex digits, got "nope"'

    PayloadTooLarge:
      description: The body exceeded 64 kb
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            error: request body too large

    InternalServerError:
      description: Unexpected failure
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            error: internal server error
